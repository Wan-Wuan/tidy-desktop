# Tidy Desktop 打包发布全流程

> 本文档记录 v2.3.0（2026-09-07）实际走通的完整发布流程：Windows 安装包 → GitHub + Gitee 双平台 Release。
> 所有命令均在项目根目录 `D:\2401348017wwa\Code\tidy_desktop` 下、PowerShell 中执行。
> 旧版流程文档见 `1打包流程.md`（仅 GitHub、单版本号时代，已过时）。

---

## 0. 前置检查

```powershell
# 工作区必须干净（release 脚本和打包都要求），先提交所有待发布改动
git status --short

# 确认远程都有：origin = GitHub，gitee = Gitee
git remote -v

# ⚠️ 关闭正在运行的 tidy-desktop 和 npm run electron:dev 开发实例
#    （dev 实例会锁定 dist/，导致构建时清理旧产物失败）
```

## 1. 提交待发布代码

```powershell
git add <改动文件>
git commit -m "<feat/fix: 改动说明>"
```

## 2. 升版本号

直接改 `package.json` 和 `package-lock.json`（顶层 `version` 与 `packages[""].version` 两处），
**不要用 `npm run release`**——它会把版本号再自动 bump 一级并重新构建。

版本号必须与后续的 tag 名（`v<版本>`）、安装包文件名（`tidy-desktop-Setup-<版本>.exe`）三者一致。

## 3. 清理旧构建产物

`dist/` 和 `release/` 都是 gitignore 的构建产物，直接删：

```powershell
Remove-Item -Recurse -Force dist\renderer, dist\main, dist\win-unpacked, `
  release\win-unpacked, release\win-unpacked.tmp -ErrorAction SilentlyContinue
```

**坑**：
- `dist/win-unpacked` 若残留（约 375MB），会被 `files: dist/**/*` 规则整个打进安装包，
  体积暴涨一倍（2.1.0 就中过招：260MB → 正常 91MB）。打包前务必确认它不存在。
- 本项目的 `scripts/clean-main-build.mjs`（`npm run build:main` 的第一步）在部分环境下
  `fs.rmSync` 会永久挂住，所以手动删干净再构建，别依赖它。

## 4. 构建并打包

```powershell
npm run typecheck          # 类型检查（可选但建议）
npm run test               # 单测（7 个文件 25 个用例，约 3 秒）
npm run electron:build     # vite build + tsc 主进程 + electron-builder
```

产物在 `release/`：

| 文件 | 说明 |
|---|---|
| `tidy-desktop-Setup-<版本>.exe` | NSIS 安装包（当前配置 maximum 压缩 + 关闭 blockmap，约 91MB） |
| `tidy-desktop-Setup-<版本>.exe.sha256` | 校验文件（第 5 步生成） |
| `latest.yml` | 版本清单（version/sha512/大小），更新检测用 |

配置在 `electron-builder.yml`：`compression: maximum` + `differentialPackage: false`，
目的是把安装包压进 Gitee 100MB 附件上限；更新器走完整下载 + SHA256 校验，不依赖 blockmap。

## 5. 生成 SHA256 校验文件

```powershell
node -e "const fs=require('fs'),c=require('crypto');const n='tidy-desktop-Setup-2.3.0.exe';const p='release/'+n;fs.writeFileSync(p+'.sha256',c.createHash('sha256').update(fs.readFileSync(p)).digest('hex')+'  '+n+'\n')"
```

（把 `2.3.0` 换成当前版本号。）

## 6. 校验产物

```powershell
# latest.yml 里的 version / 大小要和安装包对得上
cat release\latest.yml

# （可选）确认主进程、渲染器都已打进 app.asar
npx @electron/asar list release\win-unpacked\resources\app.asar | findstr /C:"dist\\main\\main\\index.js" /C:"dist\\renderer\\index.html"
```

## 7. 打标签并推送到双平台

```powershell
git add package.json package-lock.json
git commit -m "release: v<版本>"
git tag v<版本>

git push origin master v<版本>     # GitHub
git push gitee master v<版本>      # Gitee
```

推送偶发 `Failed to connect to github.com:443`，重试一次即可。

## 8. GitHub Release

前提：`gh auth login` 已登录。

```powershell
gh release create v<版本> `
  "release/tidy-desktop-Setup-<版本>.exe" `
  "release/tidy-desktop-Setup-<版本>.exe.sha256" `
  --title "Tidy Desktop v<版本>" `
  --notes-file <发布说明.md>
```

- 95MB 上传约需 4~6 分钟，建议放后台跑。
- 当前更新器的 GitHub 兜底源依赖 release 资产自带的 `digest` 字段，无需手动填。

## 9. Gitee Release

前提：`GITEE_TOKEN`（Gitee → 设置 → 私人令牌，勾选 `projects` 权限）。

```powershell
$env:GITEE_TOKEN = "<你的私人令牌>"
npm run publish:gitee
```

脚本（`scripts/publish-gitee-release.mjs`）会自动：按 tag 创建 Release（已存在则复用）→
上传 `exe` + `sha256`（超过 100MB 的附件自动跳过）→ 输出发布链接。

发布说明可以用 `scripts/sync-gitee-metadata.mjs` 从 GitHub 同步：

```powershell
GITEE_TOKEN=<令牌> node scripts/sync-gitee-metadata.mjs
```

## 10. 发布后验证

```powershell
# GitHub 侧
gh release view v<版本> --repo Wan-Wuan/tidy-desktop

# Gitee 侧（匿名 API 即可查）
curl "https://gitee.com/api/v5/repos/wanwuan/tidy_desktop/releases/tags/v<版本>"
```

确认两边都有 `exe` + `.sha256` 附件。应用内更新检测以 **Gitee 为主源、GitHub 兜底**，
Gitee Release 补齐前已安装用户不会收到升级提示。

---

## 踩坑记录（环境相关）

1. **WorkBuddy 沙箱的"安全删除"钩子**：`NODE_OPTIONS` 注入 `genie-safe-delete.cjs`，
   会把 `fs.rm` 改成移入回收站且失败即抛错 → vite 清空 `dist`、electron-builder 清理
   `release\win-unpacked.tmp` 都会失败。在 WorkBuddy 里跑构建需置空 `NODE_OPTIONS=`
   并手动预删目录；在用户自己的终端无此问题。
2. **`fs.rmSync(dir, {recursive:true})` 可能永久挂住**（Windows + 上述环境），
   手动 `Remove-Item` 后再构建可绕过。
3. **dev 实例锁文件**：`electron:dev` 运行时 dist/main 被 tsc/electron 占用，先停再打包。
4. **GitHub CLI 登录失效**：`gh auth status` 显示未登录时，先试
   `gh auth token`——若能取到令牌，用 `$env:GH_TOKEN = gh auth token` 即可继续操作。
5. **Gitee 令牌 ≠ git 凭证**：凭据管理器里 `git:https://gitee.com` 存的密码不能当
   API `access_token` 用（401），发布脚本必须用私人令牌。
