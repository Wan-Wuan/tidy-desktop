# 拖拽改造方案：统一到左键

日期：2026-09-18
目标版本：v2.8.0（本次改动需**先于** App.tsx 拆分进行）

---

## 一、要达成什么

1. **左键拖拽变得灵敏** —— 用现在右键那套自绘引擎（不受浏览器节流），替换现在左键的 HTML5 拖拽。
2. **左键能把文件拖出去**给微信 / 浏览器等外部应用发送上传（Electron 原生拖拽）。
3. **右键不再承担拖拽**，只保留右键菜单。
4. 交互最终**只有一套**：左键。

用户明确要求：*"先将右键实现拖拽的代码改为左键，然后移除右键拖拽功能。注意：保证右键显示菜单功能不变。"*

---

## 二、现状：三套引擎并存

| 触发 | 引擎 | 用途 | 位置 |
| --- | --- | --- | --- |
| 左键 | HTML5 `draggable` | 仅排序（**迟钝**） | `AppCard.tsx` 的 `draggable` + `App.tsx:1305-1375` 的 `handleCardDrag*` |
| 右键 | 自绘：`mousedown` + `mousemove` + `elementFromPoint` | 排序 + 移动到分类/子分类（**灵敏**） | `App.tsx:390-490` 的 effect + `App.tsx:1283` `handleCardMouseDown` |
| 右键（仅图片/文档） | Electron 原生拖拽 `startDragFile` | 拖到外部应用 | `App.tsx:108` `nativeDragPathRef` + `App.tsx:296-316` 的 mousemove 监听 |

关键代码：

- `App.tsx:1283` `handleCardMouseDown` —— 开头就是 `if (e.button !== 2) return`，**这就是"右键才走灵敏引擎"的开关**。
- `App.tsx:296-316` —— 原生拖拽的 mousemove 监听。注意它的注释写的是"左键拖拽图片/文档文件"，但实际由右键触发，**注释是错的**。
- `App.tsx:467-468` `suppressAppContextMenuRef` —— 因为右键拖拽释放后 Windows 仍会派发 `contextmenu`，所以拖完要临时屏蔽一次菜单。**改成左键后这套屏蔽逻辑就不需要了。**

---

## 三、目标架构

```
卡片 mousedown（左键 button === 0）
  ├─ 图片 / 文档（canNativeDrag） → 首次 mousemove 超阈值 → startDragFile() 原生拖拽（拖到微信/浏览器）
  └─ 应用 / 文件夹 / Steam      → 首次 mousemove 超阈值 → 自绘幽灵卡片 + elementFromPoint 定位
                                                       → mouseup 判定落点：应用=重排、分类/子分类=归类
右键
  └─ 只出上下文菜单（onContextMenu），不参与任何拖拽
```

「外部文件拖入窗口导入」这条链路**保持不变**（走 `<main>` 的 `dropZoneRef` / `handleDragOver` / `handleDrop`，见 `App.tsx:1908-1911`），它由系统发起、与内部引擎无关。

---

## 四、逐项改动清单

### 1. 把自绘引擎改到左键

`App.tsx:1283` `handleCardMouseDown`：

```ts
// 现在
if (e.button !== 2) return
// 改为
if (e.button !== 0) return
```

同时把 `rightDragRef` / `rightDragTargetRef` / `handleRightDragMove` / `handleRightDragUp` 改名为 `leftDrag*`（纯改名，逻辑不动），避免后续维护时被"right"误导。

⚠️ 左键 mousedown 必须 `e.preventDefault()`，否则会触发文本选中与浏览器默认图片拖拽。原生拖拽分支现在已有 `preventDefault()`，应用/文件夹分支要补上。

### 2. 拖拽结束后要抑制「点击打开」

左键松手后浏览器还会派发一次 `click`，会误触发 `handleCardClick`（打开应用）。

新增一个 `suppressNextCardClickRef`：
- 在 `handleRightDragUp`（改名后 `handleLeftDragUp`）里、判定 `active === true` 时置 `true`；
- 在 `handleCardClick`（`App.tsx:1217`）开头：若为 `true` 则立刻置回 `false` 并 `return`。

这一点现有右键实现不需要（右键不产生 click），所以是**新增的坑**，务必处理。

### 3. 文件拖到外部应用

现有 `App.tsx:296-316` 的监听**逻辑可直接复用**，只需确认它读取的 `nativeDragPathRef` 现在由左键 mousedown 写入。同时修正那段注释（现在写的是"左键"，但行为是右键，改完才名副其实）。

`startDragFile` 的 IPC 与主进程实现无需改动（`C1` 已给它加了来源与路径校验）。

### 4. 拆掉 HTML5 内部拖拽

`AppCard.tsx`：移除 `draggable`，以及 `onDragStart` / `onDragOver` / `onDragLeave` / `onDrop` / `onDragEnd` 这几个**内部排序用**的 props。

`App.tsx`：删除 `handleCardDragStart`（1305）、`handleCardDragOver`（1329）、`handleCardDrop`（1341）、`handleCardDragEnd`（1358），以及对应的 `cardOnDrag*` 稳定回调与 props 传递（`App.tsx:2379-2382`）。

⚠️ **但组级与分类级的 HTML5 处理器要保留**（`App.tsx:1572/1590`、`2083/2095`、`2229/2311`）——它们同时服务「外部文件拖入」。需要做的是把其中**内部重排的分支**去掉，只留外部文件逻辑。判断方式：`e.dataTransfer.types.includes('Files')` 才算外部拖入。

### 5. 右键只留菜单

- 删除 `suppressAppContextMenuRef`（`App.tsx:368`）及其在 `handleCardContextMenu`（1295-1303）与拖拽释放处的使用。
- `handleCardContextMenu` 简化成：`e.preventDefault()` + `openAppContextMenu(e, app)`。
- **验收重点**：右键卡片仍能弹出完整菜单（打开、以管理员身份运行、复制路径、移动分类、隐藏、删除…），右键空白处仍能弹出分类菜单。

### 6. 更新代码注释

拖拽那块有几处注释在改造后会变成误导（例如 `App.tsx:296` 说"左键"实际是右键、`AppCard.tsx` 里关于 HTML5 拖拽的说明）。**注释必须跟着改**——这块代码的价值几乎全在注释里。

---

## 五、必须原样保留的既有约束

改这块时**逐条核对**，不要因为"看起来多余"而删掉：

1. `clearDragState()` 必须在任何 `await` **之前**调用。（源卡片 DOM 被重建后 `dragend` 会丢，事后清理来不及，幽灵贴图会永久留在屏幕上。）
2. 落点 plan（`groupInsertPlanRef`）必须**先读后清**。（先清会让每次 drop 退化成"追加到末尾"。）
3. `dragOverGroupRef` / `dragOverAppRef` / `leftDragTargetRef` 这些 ref 守卫不能去掉。（`mousemove` 每帧 setState 会把网格卡死。）
4. 看门狗定时器（30s）必须保留并在卸载时清除。
5. `appsRef.current` 必须在任何 `await` 之前更新。
6. `AppCard` 的 12 个回调必须继续走 `useStableCallback`，`isSelected` 必须继续用 `Set`（见 `hooks/useStableCallback.ts`）。改造后新增/删除回调时**不要破坏这条**。

---

## 六、验证清单

改完必须**真机跑**（`npm run electron:dev`），逐项手动过：

- [ ] 左键拖动应用卡片排序：幽灵跟手、落点准确、明显比改造前灵敏
- [ ] 左键拖到另一个分类 / 子分类上：正确归类
- [ ] 左键拖动后**不会**意外打开应用（点击抑制生效）
- [ ] 单纯左键单击：正常打开应用
- [ ] 单纯左键双击 / Ctrl 多选 / Shift 范围选择：不受影响
- [ ] 左键拖动图片或文档卡片 → 拖到微信窗口 / 浏览器 → 能发送或上传
- [ ] 从资源管理器拖文件进窗口：仍能导入为新应用
- [ ] **右键卡片：菜单完整可用**
- [ ] **右键分类 / 空白处：菜单完整可用**
- [ ] 三种排序模式（manual / name / launchCount）下拖拽行为都正确
- [ ] 键盘导航（方向键 / Enter / F2）不受影响
- [ ] 快速拖动后立刻松手：不残留幽灵贴图

自动化部分：`npm run typecheck`（两条都要过）、`npm test`（当前基线 16 文件 / 81 用例）。

---

## 七、顺序

**这一步必须在 App.tsx 拆分之前完成。**

原因：拆分的第 6 步就是将这套拖拽代码抽成 `useDragAndDrop()`。如果先拆再改，等于把同一块代码翻两遍，而且拆的过程中还要同时应付行为变化，无法判断性能是变好还是变差。

建议顺序：

1. 本次拖拽改造（本方案）
2. 真机验证（上面清单）
3. App.tsx 拆分（原计划 E3）
4. 打包 + 发布 v2.8.0
