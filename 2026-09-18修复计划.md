# 修复计划

基于 `2026-09-18优化建议.md` 的审查结论制定。每条都标了**具体文件:行号**和**验收标准**，可以直接照着做。

## 总体节奏

| 版本 | 内容 | 性质 | 风险 |
| --- | --- | --- | --- |
| **v2.7.3** | A1 更新日志 / A2 闭包 bug / A3 memo + 死代码 | 补丁版，用户可感知 | 低 |
| **v2.8.0** | B 数据安全 / C 安全加固 / D 工程化 | 质量版 | 中 |
| **v2.9.0** | E App.tsx 拆分 + 测试体系 | 重构版 | 中高 |

**顺序上有一条硬约束**：A3（memo）必须在 E（拆分）之前完成。否则拆分过程中拖拽性能会一直处在「本来就很慢」的状态，你无法判断重构到底是变快了还是变慢了。

---

# v2.7.3 · 补丁版

## A1 · 让更新安装不再是黑洞

**涉及**：`src/main/update/assistant.ts`、`src/main/update/installer.ts`、`src/main/handlers/systemHandlers.ts:514-519`、`src/renderer/src/components/modals/SettingsModal.tsx`

**现状梳理**（已确认的调用链）：

```
runInstaller (installer.ts:34)
  └─ spawn 自身 exe --tidy-update-install → child.on('spawn') → done({success:true}) → 300ms 后 app.exit(0)
       └─ runUpdateAssistant (assistant.ts:90)
            ├─ waitForPidExit(pid, 120s)         ← 失败也不记
            ├─ fs.existsSync(installerPath)      ← 不存在只 console.error，而 stdio:'ignore' 直接丢弃
            └─ spawn(installer, /S --force-run /D=...) → .unref()  ← 退出码完全不看
```

全链路**没有一处落盘**，而 `systemHandlers.ts:515` 却去读 `%TEMP%\tidy-desktop-install.log`。

**改法**

1. **新建 `src/main/update/installLog.ts`**

   ```ts
   export const INSTALL_LOG_FILE = path.join(app.getPath('temp'), 'tidy-desktop-install.log')
   export function installLog(step: string, detail?: unknown): void
   ```

   - 单行格式：`[ISO 时间] [pid] 步骤 | 详情`
   - 用 `fs.appendFileSync` 且整个函数包 try/catch（**日志本身绝不能抛**）
   - 超过 256KB 时截断保留最后 64KB，防止无限增长
   - 首次写入时带一个说明表头（版本号、OS、installDir）

2. **`assistant.ts:90-103` 逐点打桩**

   | 位置 | 记录内容 |
   | --- | --- |
   | 函数入口 | argv / installerPath / installDir / waitPid |
   | `waitForPidExit` 返回后 | `exited` 还是 `timeout`（超时要记为 warn） |
   | 第 93 行 existsSync 失败 | `installer missing` + 路径，**并写 error 级别** |
   | spawn 前 | 完整命令行 |
   | **新增** `child.on('exit', code)` | 安装器退出码 |
   | **新增** `child.on('error', err)` | spawn 失败原因 |

   注意：现在 `.unref()` 后立即返回，拿不到 exit。改成**不 unref**，让助手进程活到安装结束——反正它没有窗口，开销可忽略，而且这样才能记录退出码。

3. **`installer.ts:74-80`**：在 `child.on('spawn')` 里补一条 `installLog('assistant spawned, main exiting')` **再** `app.exit(0)`。这条是为了保证主进程被杀之前日志已经落盘。

4. **`systemHandlers.ts:514-519` 改返回值**

   现在文件不存在就 `return false`，渲染层无法区分「没日志」和「打开失败」。改为：

   ```ts
   return { ok: false, reason: 'not-found' }
   ```

   渲染层据此提示「暂无更新安装记录（说明本次更新没有走到安装步骤）」。

5. **启动时读取日志尾部做提示**

   新增一个 `readLastInstallStatus()`：若最后一条是 `assistant spawned` 而**没有**对应的 `installer exited`，说明上次更新中断了 → 在设置页显示「上次更新可能未完成」+ 打开日志按钮。

6. **渲染层**：`SettingsModal.tsx` 里「打开更新日志」的返回处理要跟着改（现在只判断 boolean）。

**验收**

- [ ] 正常走一次更新 → 日志有完整时间线，最后一行是 `installer exited | 0`
- [ ] 手动取消 UAC → 日志有非 0 退出码
- [ ] 更新前把 `%TEMP%\tidy-desktop-update.exe` 删掉 → 日志有 `installer missing`
- [ ] 首次安装（无日志文件）→ 点「打开更新日志」有明确提示，不是无反应
- [ ] `npm run typecheck && npm run build:main && npm run test`

**风险**：低，纯增量。唯一要盯的是助手进程生命周期变长后不影响安装器运行。

---

## A2 · 修右键拖拽的过期闭包

**涉及**：`src/renderer/src/App.tsx:347-454`

`useEffect` 依赖数组在 **454 行是 `[]`**，但内部调用 `handleReorderApp`（1662 行），后者在 **1677 行读 `config?.ui?.sortMode`**。首渲染 `config` 为 `null`，闭包永久锁死在 `'manual'`。

**改法（推荐用 ref 转发，不要把函数塞进依赖数组）**

```ts
const handleReorderAppRef = useRef(handleReorderApp)
useEffect(() => { handleReorderAppRef.current = handleReorderApp }, [handleReorderApp])
// effect 内部改调 handleReorderAppRef.current(...)
```

理由：`handleReorderApp` 依赖 `appsRef`、`config`、`handleMoveAppToSubcategory` 三样，直接进依赖数组会让这个监听 `mousemove`/`mouseup` 的 effect 频繁重绑，反而引入新问题。

顺带检查同一 effect 内其它状态读取：`rightDragRef`、`dragOverGroupRef`、`groupInsertPlanRef` 都是 ref，安全。

**验收**

- [ ] 设置切到「按名称」→ 右键拖一个应用盖到另一个上：**不应**改变数组顺序（只改归属）
- [ ] 切回「手动」→ 拖拽排序恢复正常
- [ ] 跨子分类拖拽 + 三种排序模式各回归一遍

---

## A3 · 让 AppCard 的 memo 真正生效

**涉及**：`src/renderer/src/App.tsx`（调用处 2319-2340 + 各 handler 定义）、`src/renderer/src/components/AppCard.tsx`

`AppCard.tsx:17` 的 `React.memo` 被完全击穿。调用处（2319-2340）传 16 个 props，其中：

**9 个普通函数需要 `useCallback`**
`handleCardClick`(1142)、`handleCardMouseDown`(1208)、`handleCardContextMenu`(1220)、`handleCardDragStart`(1230)、`handleCardDragOver`(1254)、`handleCardDrop`(1266)、`handleCardDragEnd`(1283)、`handleCardKeyDown`(1311)、`handleSendFile`(655)

**3 个内联箭头需要提出来**
`onEdit`(2328)、`onDelete`(2329)、`onDragLeave`(2335) → 定义成 `handleCardEdit` / `handleCardDelete` / `handleCardDragLeave`

**1 个 O(n²) 需要换实现**
`isSelected={selectedAppIds.includes(app.id)}`(2326) → `const selectedIdSet = useMemo(() => new Set(selectedAppIds), [selectedAppIds])`，传 `selectedIdSet.has(app.id)`

**验证 memo 真的生效**：临时在 AppCard 顶层加 `console.count('AppCard render')`，拖拽时应保持稳定，不随 mousemove 暴涨。**确认后删掉这行。**

**验收**

- [ ] React DevTools Profiler 里，拖拽时 AppCard 的 render 次数显著下降
- [ ] 60+ 应用的分类下拖拽不掉帧
- [ ] 卡片点击、右键菜单、F2 编辑、键盘导航、拖拽归类——逐项手动回归

**风险**：中低。`useCallback` 依赖写错会导致行为悄悄变化，**每个 handler 改完就立即回归对应交互**，不要攒到最后一起测。

---

## A4 · 顺手清死代码

**涉及**：`src/renderer/src/App.tsx:2119-2168`

`<section className="hidden">` 是一整块页头概览+智能启动的复制品，被 Tailwind 永久隐藏，里面还夹着一次 `smartLaunchApps` 的重复遍历。

**验收**：删掉后 `npm run build` 通过；界面无变化（因为它本来就不显示）；确认 `smartLaunchApps` 若因此变成未使用则一并清理。

---

# v2.8.0 · 质量版

## B1 · 原子写补 fsync + 启动恢复

**涉及**：`src/main/jsonTransaction.ts`、`src/main/config.ts`

现状：`jsonTransaction.ts:27-31` 是「rename 原→.bak，rename tmp→原」，**无 fsync**，回滚只在同进程 catch 内有效。若在两次 rename 之间被杀，数据文件只存在于 `.bak`，**启动无恢复**。备份文件名含 transactionId（`${filePath}.${tid}.bak`），所以要按模式匹配。

**改法**

1. 每个 `writeFileSync` 之后补 `fsyncSync`：
   ```ts
   const fd = fs.openSync(file.tempPath, 'r+')
   fs.fsyncSync(fd); fs.closeSync(fd)
   ```
2. 新增 `recoverInterruptedWrites(dir): number`：
   - 扫描 `*.tmp` 和 `*.bak`
   - 若 `<name>.<tid>.bak` 存在且同名主文件缺失 → 恢复成主文件
   - 清理无法配对的残留
   - 返回处理条数并写日志
3. 在 `ensureDataDir()`（`config.ts:13`）里调用一次，且**必须在任何 readJsonFile 之前**。
4. ⚠️ 注意 `config.ts:62-64` 的 `writeJsonFile`用的是 `filePath + '.tmp'`（**不带 tid**），与 jsonTransaction 的命名不同——恢复逻辑要同时覆盖两种。

**验收**

- [ ] 单元测试：构造「主文件缺失 + .bak 存在」场景，断言恢复成功
- [ ] 构造「.tmp 存在 + 主文件正常」→ 应清理 tmp 且不动主文件
- [ ] 手工把 apps.json 删掉、留一个 .bak → 启动后数据恢复

## B2 · 损坏后不再静默覆盖

**涉及**：`src/main/config.ts:38-57`、`src/main/handlers/fileHandlers.ts`、渲染层

现状：`readJsonFile` 解析失败只落一个 `.corrupt-*` 就返回默认值，**没有任何 UI 入口**，随后 `save-apps` 就把默认值覆盖写回去——原始数据虽然在 `.corrupt-*` 里，但用户根本不知道。

**改法**

1. `readJsonFile` 增加「损坏标记」返回（或模块级 `Set<string>` 记录损坏文件路径）。
2. `save-apps` / `save-categories` / `save-config` 在本次会话内**拒绝对已损坏文件写入**，返回 `{ ok: false, reason: 'corrupted' }`。
3. 渲染层收到后弹明确提示：「数据文件损坏，已备份到 `<路径>`，本次不会覆盖」。
4. 设置 → 数据 增加「从损坏备份恢复」：列出 `*.corrupt-*`，让用户选择恢复哪个。
5. 启动时若发现 `*.corrupt-*` 存在，给一次性提示。

**验收**

- [ ] 手动把 `apps.json` 改成非法 JSON → 启动有提示、不覆盖、`.corrupt-*` 存在
- [ ] 从设置里能选到该备份并恢复成功

## B3 · 主进程同步阻塞改异步

**涉及**：`src/main/config.ts:20-35`、`src/main/handlers/systemHandlers.ts:301-315`、`src/main/handlers/iconHandlers.ts:94-141`、`61`

| 位置 | 现状 | 改法 |
| --- | --- | --- |
| `config.ts:20-35` | 启动时 icons 目录逐个 `statSync` | 延后到 `whenReady` 之后异步执行；或改 `fs.promises` |
| `systemHandlers.ts:301-315` | 最多 5000 次同步 `existsSync` | `fs.promises.access` + 分批 `Promise.all`，并发上限 32 |
| `iconHandlers.ts:94-141` | `execFileSync('powershell')`，4s 超时，逐个调用 | 改 `execFile` 异步 + 并发上限 4，保留 4s 超时 |
| `iconHandlers.ts:61` | `execFileSync('reg')`，2s | 改异步 |

**验收**：导入 300 个快捷方式时，Performance 面板中主线程长任务 < 50ms；界面不冻结。

---

## C1 · 统一 IPC 校验

**涉及**：新建 `src/main/ipcGuard.ts`；改 `appHandlers.ts`、`iconHandlers.ts`、`systemHandlers.ts`、`update/index.ts`

现状：全项目 **49 处 IPC 注册，只有 3 处校验了 `event.sender`**（`systemHandlers.ts:99/127/172`，都是窗口 resize）。`validation.ts` 只覆盖 config/apps/categories。

**改法**

1. 新建 `ipcGuard.ts`：
   - `assertSender(event)`：校验来源是自家窗口（维护 windowId 集合 + `isAllowedInternalUrl(event.senderFrame.url)`）
   - `assertPath(value, opts)`：字符串 + 长度上限 + 是否绝对路径 + 存在性；可选扩展名白名单
2. **先做高风险子集**（不必一次铺满 49 处）：

   | 风险 | handler | 位置 |
   | --- | --- | --- |
   | 提权 | `open-app-as-admin` | `appHandlers.ts:114` |
   | 任意程序启动 | `open-app` / `open-folder` / `show-item-in-folder` / `open-containing-folder` | `appHandlers.ts:76/85/96/131` |
   | 任意文件读写 | `copy-file-to-clipboard` / `copy-image-to-clipboard` / `extract-icon` | `appHandlers.ts:228/242`、`iconHandlers.ts:172` |
   | 安装任意 exe | `install-update` | `update/index.ts:257` |
   | 系统命令 | `run-quick-action` | `appHandlers.ts:183` |
   | 拖拽导出 | `start-drag-file` | `systemHandlers.ts:521` |

3. `open-app-as-admin` 额外要求扩展名在 `.exe/.bat/.cmd/.lnk` 白名单内。

**验收**：每个改动过的 handler 用渲染层正常路径回归一遍；构造一个非法路径（如 `..\..\Windows\System32\cmd.exe`）断言被拒。

## C2 · 窗口安全收口

**涉及**：`src/main/index.ts:112-122`、`186-191`

1. **显式声明 `sandbox: true`**（当前 `webPreferences` 里没有这个键，靠默认值）。
   已确认 `preload.ts` **没有引入任何 Node 模块**（只用 `contextBridge`/`ipcRenderer`/`webUtils`），所以开启 sandbox 是安全的。
2. **收敛 `file:` 放行范围**：`isAllowedInternalUrl` 生产分支现在是 `return url.protocol === 'file:'`——本地任意 HTML 都能导航进来复用 preload 权限。改为限定在自身 renderer 目录：

   ```ts
   const rendererRoot = path.resolve(__dirname, '../renderer')
   return url.protocol === 'file:' &&
     path.resolve(decodeURIComponent(url.pathname)).startsWith(rendererRoot)
   ```

**验收**

- [ ] 开 sandbox 后：拖拽取路径、搜索窗唤起、图标提取、更新检查全部正常
- [ ] 控制台执行 `location.href = 'file:///C:/Windows/win.ini'` → 被拦截

---

## D1 · 引入 ESLint + Prettier

**涉及**：新建 `eslint.config.js`、`.prettierrc`、`.editorconfig`；改 `package.json`

已确认这四个配置文件**都不存在**，而 `tsconfig.json` 还关着 `noUnusedLocals`/`noUnusedParameters`。

- ESLint 9 flat config + `typescript-eslint` + **`eslint-plugin-react-hooks`**（这条最关键——它能直接把 A2 那类闭包/依赖问题在编辑期抓出来）
- 首轮规则设 `warn`：`no-unused-vars`、`react-hooks/exhaustive-deps`、`react-hooks/rules-of-hooks`
- 先跑一次把基线输出到文件，**不要一次性修完**，按批次增量清理
- Prettier 只加配置，先 `--check` 摸底，格式化分批做，避免一个 PR 淹没所有 diff
- `package.json` 加 `"lint"` / `"format"` 脚本

**验收**：`npm run lint` 能跑通并输出基线；CI 里先设为不阻断（warn）。

## D2 · typecheck 拆成两条

**涉及**：`package.json`、新建 `tsconfig.renderer.json`

现状：`npm run typecheck` 走 `tsconfig.json`（ESNext + bundler + DOM），而主进程真实编译走 `tsconfig.main.json`（CommonJS）——**两边环境不一致可能漏报**；`tsconfig.node.json` 没有任何地方引用。

```
typecheck:main     → tsc -p tsconfig.main.json --noEmit
typecheck:renderer → tsc -p tsconfig.renderer.json --noEmit   （新建，含 DOM）
typecheck          → npm run typecheck:main && npm run typecheck:renderer
```

顺带给 `tsconfig.node.json` 找到归宿（`vite.config.ts` 用）或直接并掉。

**验收**：`npm run typecheck` 两条都通过；故意在主进程写一个只在新配置下报错的用例验证它确实生效。

## D3 · 仓库信息收敛到一处

**涉及**：新建 `scripts/lib/repo.mjs`；改 4 个脚本

`owner`/`repo` 硬编码散在：`publish-github-release.mjs:15-16`、`publish-gitee-release.mjs:4-5`、`sync-gitee-metadata.mjs:6-8`、`gitee-releases.mjs:21-22`（大小写还不一致：`Wan-Wuan` vs `wanwuan`）。

**验收**：4 个脚本改为 import；发布脚本干跑一遍确认 URL 正确。

## D4 · release.mjs 补强

**涉及**：`scripts/release.mjs`

- `135` 行打 tag 失败时**回滚 commit**（现在只在 commit 前回滚，会留下「已提交无 tag」的半成品）
- 增加 CHANGELOG 校验：缺当前版本条目则警告（或阻断）
- 清理 `release/` 里的旧产物
- `package.json` 补 `"publish:github"`（脚本已存在但没有入口）

## D5 · 加 CI

**涉及**：新建 `.github/workflows/ci.yml`

现状 `.github/` 目录不存在，全靠本地手动。

- `ci.yml`：push/PR 到 master 跑 `npm ci` → `typecheck` → `test` → `build`
- **发布仍走本地**——Gitee 没有官方 runner，硬上 CI 反而更复杂。可选加一个 `workflow_dispatch` 手动触发的 `release.yml` 只发 GitHub。

**验收**：往分支推一次，Actions 绿。

---

# v2.9.0 · 重构版

## E1 · 拆分 App.tsx（每步一个 commit）

按这个顺序，**每步都能单独验证**：

| 顺序 | 抽出 | 对应区域 | 验收要点 |
| --- | --- | --- | --- |
| 1 | `useAppData` | 62-66、192-227、540-581 | 收敛 `apps`/`categories`/`activeCategory` 三组 ref 镜像（约 40 处手动双写） |
| 2 | `useUndoSnapshot` | 92、150-158、1696-1716 | 撤销/重做仍准确 |
| 3 | `useAppSelection` | 326-327、1142-1206 | 多选、框选、批量删除 |
| 4 | `useCategoryDialogs` | 322-324、1330-1402 | 分类/子分类增删改 |
| 5 | `useIconBackfill` | 494-538 | 图标补齐、缺失图标提示 |
| 6 | `useDragAndDrop` | 85-91、94-124、347-454、1230-1294、1543-1660 | 两套拖拽全回归 |
| 7 | 展示组件 | 1832-2456 | `HeaderOverview` / `CategoryNav` / `AppGrid` / `SelectionBar` / `ToastStack` |

**目标**：App.tsx 从 2398 行降到约 500 行。
**注意**：第 6 步最高风险，建议**单独一个版本或单独一个 PR**，并且改完后必须重跑 A3 的 Profiler 验证。

## E2 · 纯函数抽 utils + 补测

这些是最容易改坏又最没有保护的地方，而且都是纯函数，**抽出来补测成本很低（不需要 jsdom）**：

| 目标 | 现在位置 | 去向 |
| --- | --- | --- |
| `sortAppsForDisplay` | `App.tsx:1410` | `utils/sortApps.ts` + test |
| 搜索匹配与打分 `matchesTerm` / `getSearchScore` | `SearchApp.tsx:277`、`297` | `utils/searchScore.ts` + test |
| 重排插入位置计算 | `App.tsx:1662-1694` | `utils/reorder.ts` + test |

`reorder` 的测试至少要覆盖：source 在 target 之前 / 之后、跨子分类、三种 `sortMode`。

## E3 · 测试基础设施

**涉及**：`vitest.config.ts`（现在只收 `*.test.ts`）、新增 `jsdom` + `@testing-library/react`

先给 `AppCard` 写一个「props 不变则不重渲染」的测试——**正好锁住 A3 的修复**，防止以后有人又传个内联箭头进去。

---

## 收尾小项（可挂在任意版本）

- `README.md:129-130` 安装示例还写着 v2.2.0，更新到当前版本
- `design-qa.md` 结尾是 `blocked`：用 Playwright 把三套模板在 1440×1024 下截图，比对后正式收尾并归档
- `docs/` 下的历史预览 HTML、`docs/superpowers/` 计划文件归档
- `@types/node ^25` 与 Electron 43 内置 Node 版本对齐
- a11y：抽 `useDialogA11y` 覆盖 `AddAppModal` / `SettingsModal` / `SmartOrganizeModal`；修 `App.tsx:338-340` 让 Esc 能真正关掉弹窗；两个右键菜单补 `role="menuitem"` 与方向键

---

## 明确不做（这一轮）

- **长列表虚拟化**：先看 A3 修完后的实测效果。几百张卡片若已够快，虚拟化是纯粹的复杂度负担。
- **index.css 里的 29 处 `!important`**：多为主题变量覆盖与焦点环保护，属可接受技术债，清理收益低风险高。
- **全自动双渠道 CI**：Gitee 无官方 runner，收益不抵复杂度。

---

## 回归清单（每个版本发布前）

```
npm run typecheck
npm run test
npm run build
npm run build:main
npm run electron:build
```

手动回归（按版本取用）：

- 拖拽：HTML5 拖拽排序 / 右键拖拽归类 / 跨子分类 / 拖入外部应用（图片、文档）
- 三种排序模式下的拖拽行为
- 搜索窗：唤起、模糊匹配、命令模式（`>`）、Esc 关闭
- 设置：三套界面模板切换、主题、窗口尺寸与位置持久化
- 更新：检查 → 下载 → 安装（含取消 UAC 的失败分支）
- 数据：备份导出/导入、诊断导出、损坏恢复
- 导入：桌面/开始菜单扫描、拖入 .lnk、去重
