下面是本项目打包并发布到 GitHub 的常用命令流程，按顺序执行即可。

**1. 检查当前修改**

```powershell
git status
```

确认哪些文件要发布。当前项目里如果要包含最近搜索框修复，至少会有：

```powershell
src\renderer\src\SearchApp.tsx
src\renderer\src\search.css
```

**2. 修改版本号**

比如从 `1.10.15` 升到 `1.10.16`：

```powershell
npm version 1.10.16 --no-git-tag-version
```

这会自动修改：

```text
package.json
package-lock.json
```

**3. 类型检查和前端构建**

```powershell
npm run typecheck
npm run build
```

**4. 打包 Windows 安装包**

本项目的安装包构建命令是：

```powershell
npm run electron:build
```

打包完成后，安装包会出现在：

```text
release\
```

例如：

```text
release\tidy-desktop Setup 1.10.16.exe
release\tidy-desktop Setup 1.10.16.exe.blockmap
release\latest.yml
```

**5. 提交代码到 GitHub**

```powershell
git add package.json package-lock.json src\renderer\src\SearchApp.tsx src\renderer\src\search.css
git commit -m "fix: improve search result layout"
git push origin main
```

如果你当前分支不是 `main`，先看分支名：

```powershell
git branch --show-current
```

然后把 `main` 换成实际分支名。

**6. 创建 GitHub Release 并上传安装包**

先确认已登录 GitHub CLI：

```powershell
gh auth status
```

如果没登录：

```powershell
gh auth login
```

创建 release，例如 `v1.10.16`：

```powershell
gh release create v1.10.16 `
  "release\tidy-desktop Setup 1.10.16.exe" `
  "release\tidy-desktop Setup 1.10.16.exe.blockmap" `
  "release\latest.yml" `
  --repo Wan-Wuan/tidy-desktop `
  --title "v1.10.16" `
  --notes "修复搜索框结果项显示问题"
```

**最短完整流程示例**

```powershell
npm version 1.10.16 --no-git-tag-version
npm run typecheck
npm run build
npm run electron:build

git add package.json package-lock.json src\renderer\src\SearchApp.tsx src\renderer\src\search.css
git commit -m "fix: improve search result layout"
git push origin main

gh release create v1.10.16 `
  "release\tidy-desktop Setup 1.10.16.exe" `
  "release\tidy-desktop Setup 1.10.16.exe.blockmap" `
  "release\latest.yml" `
  --repo Wan-Wuan/tidy-desktop `
  --title "v1.10.16" `
  --notes "修复搜索框结果项显示问题"
```

注意：发布前一定要让 `package.json` 里的版本号、tag 名、安装包文件名保持一致。