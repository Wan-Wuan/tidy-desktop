# 2026-09-13 项目改动记录

> 本日 5 个提交，发布 2 个版本：**v2.6.7 → v2.6.8**（GitHub 已上线，Gitee 待有效令牌）
> 主线：Windows 任务栏图标不一致问题彻底修复（共查出三层根因）→ 打包流程加入图标校验 → 双版本发布

---

## 一、任务栏图标不一致：问题与三层根因

**现象**：应用内/桌面快捷方式已是新的「奶油瓷收纳抽屉」图标，但 Windows 任务栏按钮
显示 Electron 默认原子图标；修了一层后又复现，最终定位到第三层才是真凶。

| 层 | 根因 | 修复 |
|---|---|---|
| 1 | `generate-icons.mjs` 手工拼的 ICO 不合规：小尺寸全用 PNG 条目（Windows 仅 256px 支持）、目录项 `wPlanes=32 / bColorCount=1` 等字段错误 | ICO 重写：16–128 走 BMP(DIB) 条目、256 走 PNG 条目；新增结构自检 |
| 2 | 主进程把 PNG 交给 `BrowserWindow`，Windows 窗口/任务栏图标不认 PNG；无边框窗口不套用构造参数 `icon` | `getAppIcon()` 优先取 `.ico`（resourcesPath → asar 逐级回退），窗口创建后显式 `setIcon()` |
| 3 | **最终真凶**：dev 调试时 Chromium 为通知功能自动创建 `开始菜单\Programs\Electron.lnk`，旧代码下它携带与发布应用**相同的 AUMID**（`com.tidy-desktop.app`）且指向 node_modules 的 electron.exe。Explorer 解析任务栏按钮图标按「AUMID → 开始菜单快捷方式匹配」，命中了这个错的，无论窗口图标/exe 内嵌图标/快捷方式图标怎么改都无效 | ① 安装器升级时自动删除残留 `Electron.lnk`；② dev 改用独立 AUMID（`com.tidy-desktop.app.dev`），从根上断绝撞车 |

**排查中的关键实验**（为什么能排除前面的层）：

- 向运行中窗口发红色测试图标（`WM_SETICON`）→ 任务栏纹丝不动 ⇒ 按钮图标不读窗口图标
- 覆盖安装 + `SHChangeNotify(ASSOCCHANGED)`、重启 Explorer、删除 `iconcache_*.db` → 均无效
  ⇒ 不是（单纯）图标缓存问题
- `SHDefExtractIcon` 按 16–256px 解码快捷方式 ICO 全部成功 ⇒ ICO 本身没问题
- 全系统搜索携带 `com.tidy-desktop.app` 的快捷方式 → 揪出 `Electron.lnk`

## 二、代码改动清单

| 文件 | 改动 |
|---|---|
| `src/main/index.ts` | `getAppIcon()` 重写：Windows 优先 `.ico`、候选名首位为版本化 `app-icon-${version}.ico`、逐个 `isEmpty()` 校验后缓存；新增 `applyAppIcon()`（无边框窗口创建后显式 setIcon）；dev/发布 AUMID 隔离 |
| `scripts/generate-icons.mjs` | 改用 `scripts/lib/{png,ico}.mjs`：BMP(DIB)+PNG 混合条目，生成后结构自检 |
| `scripts/lib/png.mjs` `scripts/lib/ico.mjs` | 新增：PNG 解码、ICO 组装/解析（目录项按规范写 `wPlanes=1 / wBitCount=32 / bColorCount=0`） |
| `scripts/check-icons.mjs` | 新增：必备尺寸、目录项字段、`icon.ico`/`app-icon.ico`/`icon-256.png`/`public/*` 同源性、exe 内嵌校验（`--exe`，打包后跑） |
| `build/installer.nsh` | 升级安装自动删除残留 `Electron.lnk`；ICO 按版本号落盘 `app-icon-${VERSION}.ico` 并让快捷方式指向它 |
| `electron-builder.yml` | `files` 增加 `build/app-icon.ico`（asar 兜底）；`extraResources` 落盘 ICO |
| `package.json` | 新增 `icons:generate` / `verify:icons`；`electron:build` 串入 `verify:icons`（不合规拦住打包） |

## 三、打包发布流程更新（`打包发布全流程.md`）

- 第 4 步：补充 `npm run icons:generate`（图标素材改动时）与 `electron:build` 内置 `verify:icons` 说明
- 第 6 步：新增打包后必跑 `node scripts/check-icons.mjs --exe`（顺序不能反，打包前会拿旧 exe 误报）
- 图标文件名版本化约定的说明（勿手工替换、勿改文件名）

## 四、发布记录

| 版本 | 构建 | GitHub | Gitee |
|---|---|---|---|
| v2.6.7 | 13:07（含 AUMID 隔离 + 版本化 ICO 安装器） | ✅ [release/tag/v2.6.7](https://github.com/Wan-Wuan/tidy-desktop/releases/tag/v2.6.7) | ⏳ 待有效令牌 |
| v2.6.8 | 14:52（用户构建；含 Electron.lnk 自动清理） | ✅ [release/tag/v2.6.8](https://github.com/Wan-Wuan/tidy-desktop/releases/tag/v2.6.8) | ⏳ 待有效令牌 |

- 两个版本的 tag 均已推送 origin + gitee
- Gitee 侧环境里的 `GITEE_TOKEN` 已失效（401），补发任一版本（发 2.6.8 即可，更新器取最高版本）需新令牌：
  `Gitee → 设置 → 私人令牌（projects 权限）` → `$env:GITEE_TOKEN = "<新令牌>"; npm run publish:gitee`
- 提交：`1d8bac0` 图标修复主体 → `fe49f9a` 流程文档 → `ccf466b` Electron.lnk 清理 + 根因修正 → `4c817cc` release: v2.6.8

## 五、本机处置记录（开发机）

- 删除残留 `Electron.lnk` → 重启 Explorer → 重启发行版，任务栏图标恢复且不再复发
- 期间清理过 `iconcache_*.db`（事后看非必需，但无副作用）
- dev 与发行版现已互不干扰：`electron:dev` 任务栏图标正常，发行版不受调试影响

## 六、经验教训

1. **Windows 任务栏按钮图标 ≠ 窗口图标**：窗口带显式 AUMID 时，按钮图标走「AUMID → 开始菜单快捷方式匹配」，这条链上的任何一环（残留快捷方式）都能劫持显示，且与窗口/exe/图标缓存全部无关。
2. **dev 与正式版必须用不同 AUMID**：否则开发期的任何图标/标识状态都会污染正式版的 Shell 解析。
3. 排查 Shell 问题时，"改了源之后看显示有没有变"不可靠（显示可能被缓存/匹配劫持），要直接验证每一环的解析结果（如 `SHDefExtractIcon`、`ExtractIconEx`、lnk 的 AUMID 属性）。
4. 相关技术细节完整版见 [2026-09-13图标一致性修复.md](./2026-09-13图标一致性修复.md)。
