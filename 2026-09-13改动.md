# 2026-09-13 项目改动记录

> 本日 10 个提交，迭代发布 3 个版本：**v2.6.7 → v2.6.8 → v2.6.9**（均已上 GitHub；Gitee 待有效令牌）
> 主线：任务栏图标一致性彻底修复（三层根因 + AUMID 劫持真凶）→ 新用户"快捷方式隐身"与更新安装失败修复 → 更新下载链路健壮性 → 打包流程加入图标校验 → 新用户全流程测试清单

---

## 一、任务栏图标不一致：问题与三层根因

**现象**：应用内/桌面快捷方式已是新的「奶油瓷收纳抽屉」图标，但 Windows 任务栏按钮
显示 Electron 默认原子图标。逐层排查发现三层叠加的问题：

| 层 | 根因 | 修复 |
|---|---|---|
| 1 | `generate-icons.mjs` 手工拼的 ICO 不合规：小尺寸全用 PNG 条目（Windows 仅 256px 支持）、目录项 `wPlanes=32 / bColorCount=1` 等字段错误 | ICO 重写：16–128 走 BMP(DIB) 条目、256 走 PNG 条目；生成后结构自检 |
| 2 | 主进程把 PNG 交给 `BrowserWindow`，Windows 窗口/任务栏图标不认 PNG；无边框窗口不套用构造参数 `icon` | `getAppIcon()` 优先取 `.ico`（resourcesPath → asar 逐级回退），窗口创建后显式 `setIcon()` |
| 3 | **最终真凶**：dev 调试时 Chromium 为通知功能自动创建 `开始菜单\Programs\Electron.lnk`，旧代码下它携带与发布应用**相同的 AUMID**（`com.tidy-desktop.app`）且指向 node_modules 的 electron.exe。Explorer 解析任务栏按钮图标按「AUMID → 开始菜单快捷方式匹配」，命中了这个快捷方式，显示 electron.exe 的原子图标——窗口图标/exe 内嵌/快捷方式图标/清缓存全都与这条链无关 | ① 安装器升级时自动删除残留 `Electron.lnk`；② dev 改用独立 AUMID（`com.tidy-desktop.app.dev`），从根上断绝撞车 |

**排查中的关键实验**：

- 向运行中窗口 `WM_SETICON` 发送纯红色测试图标 → 任务栏按钮**纹丝不动** ⇒ 按钮图标不读窗口图标
- 覆盖安装 + `SHChangeNotify(ASSOCCHANGED)`、重启 Explorer、删除 `iconcache_*.db` → 均无效
- `SHDefExtractIcon` 按 16–256px 解码快捷方式 ICO 全部成功 ⇒ ICO 本身没问题
- 全系统搜索携带 `com.tidy-desktop.app` 的快捷方式 → 揪出 `Electron.lnk`

**代码改动**：

| 文件 | 改动 |
|---|---|
| `src/main/index.ts` | `getAppIcon()` 重写（Windows 优先 `.ico`、候选名首位版本化 `app-icon-${version}.ico`、`isEmpty()` 校验后缓存）；新增 `applyAppIcon()`；dev/发布 AUMID 隔离 |
| `scripts/generate-icons.mjs` + `scripts/lib/{png,ico}.mjs` | ICO 生成重写（BMP+PNG 混合条目、字段合规、结构自检） |
| `scripts/check-icons.mjs` | 新增：尺寸/字段/同源性校验 + exe 内嵌校验（`--exe`，打包后跑） |
| `build/installer.nsh` | 升级自动删除残留 `Electron.lnk`；ICO 按版本号落盘 `app-icon-${VERSION}.ico` 并让快捷方式指向它 |
| `electron-builder.yml` | `files` 增加 `build/app-icon.ico`（asar 兜底）；`extraResources` 落盘 ICO |
| `package.json` | 新增 `icons:generate` / `verify:icons`；`electron:build` 串入 `verify:icons` |

## 二、新用户"快捷方式隐身"（诊断日志定位，已修复）

新用户（2.6.7）导入快捷方式后网格全空白但显示"84/84 可见"，能搜索启动、卡片看不见。

- **根因**：入场动画 `.stagger-enter > *` 把卡片**静止态**写成 `opacity: 0`，靠动画变可见。
  系统开启"减少动画效果"（`prefers-reduced-motion: reduce`）时 `animation: none !important`
  生效 → 卡片**永久透明**。诊断日志印证：数据完好、纯渲染层问题。
- **修复**（`index.css`）：隐藏态移入 `@keyframes` 的 `from`，静止态天然可见；
  动画被禁用时静态显示，动画正常时入场效果不变。
- 触发条件：Windows 设置 → 辅助功能 → 视觉效果 → 动画效果 = **关**（很常见）。

## 三、更新"安装没有完成"/更新失败（已修复）

- **根因**：本应用"关闭窗口 = 最小化到托盘"，进程不退出，NSIS 覆盖 `tidy-desktop.exe`
  时文件被占用 → 安装中止；应用内更新助手旧逻辑 `Wait-Process -Timeout 30` **超时后照样安装**，同样撞锁。
- **修复**：
  - `build/installer.nsh` 新增 `customCheckAppRunning` 宏：安装时请求应用退出、失败强制结束
    （重试 10 次），不依赖 PowerShell 可用性；
  - `src/main/update/installer.ts`：更新助手**真正等到进程退出**（上限 120s，超时放弃并记日志）。

## 四、更新下载链路健壮性（弱网环境，已修复）

| 缺陷 | 修复（`network.ts` / `index.ts`） |
|---|---|
| 任何网络错误都删除半成品 → 91MB 每次从头下，弱网失败率倍增 | 网络类失败保留断点，重试经 Range 续传；重试次数 3→5 |
| 断点越过文件末尾时收到 HTTP 416 → 每次重试同样失败，永久卡死 | 416 自动清掉断点、从头重下 |
| GitHub 渠道只认 API `digest` 字段，缺失即更新静默不可用 | 两渠道均支持 `.sha256` 旁路校验文件兜底 |
| 下载失败即清空缓存 → 用户再点永远从头 | 失败保留断点跨点击续传；仅版本/URL 变化或 SHA256 不匹配时清理 |

## 五、打包与发布流程改进

- `打包发布全流程.md`：第 4 步补 `icons:generate` 与 `electron:build` 内置 `verify:icons`；
  第 6 步新增打包后必跑 `node scripts/check-icons.mjs --exe`（顺序不能反）
- 新增 `docs/新用户全流程测试清单.md`：安装 → 首启 → 导入 → 日常 → 更新 → 卸载 全链路
  操作清单（含环境准备、回归标记 ⭐、已知风险点、缺陷上报模板）
- 实测踩坑：`customCheckAppRunning` 宏多写一个 `${endIf}` 会直接导致 NSIS 编译失败
  （LogicLib 的 If/EndIf 必须配对，Goto 目标尽量放在条件块外）——发版前必须完整跑一次打包

## 六、发布记录

| 版本 | 内容 | GitHub | Gitee |
|---|---|---|---|
| v2.6.7 | 图标三层修复主体（AUMID 隔离 + 版本化 ICO 安装器） | ✅ [v2.6.7](https://github.com/Wan-Wuan/tidy-desktop/releases/tag/v2.6.7) | ⏳ |
| v2.6.8 | Electron.lnk 自动清理（用户自行构建，本侧上传） | ✅ [v2.6.8](https://github.com/Wan-Wuan/tidy-desktop/releases/tag/v2.6.8) | ⏳ |
| v2.6.9 | 隐身修复 + 托盘强杀 + 更新器等待 + 下载健壮性 | ✅ [v2.6.9](https://github.com/Wan-Wuan/tidy-desktop/releases/tag/v2.6.9) | ⏳ |

- 三个版本的 tag 均已推送 origin + gitee；Gitee Release 全部待发
- **Gitee 令牌失效**（401 Access token does not exist）：新令牌（projects 权限）到位后执行
  `$env:GITEE_TOKEN = "<新令牌>"; npm run publish:gitee`——补发 v2.6.9 一个版本即可，
  更新器双平台取最高版本，已装用户即可收到更新提示

## 七、提交清单（10 个）

```
1d8bac0 fix: harden app icon consistency across taskbar, shortcuts and exe
fe49f9a Docs: add icon generate/verify steps to release flow
ccf466b fix: remove stale dev Electron.lnk that hijacked taskbar icon via AUMID match
4c817cc release: v2.6.8
d8532db Docs: add 2026-09-13 daily change log
ae8df83 fix: invisible shortcut cards under reduced motion; installer kills tray-resident app
0885ced Docs: add fresh-user full-journey test checklist
a95eb48 fix: update download reliability for flaky networks
18e19f4 release: v2.6.9
2de2e00 Docs: mark v2.6.9 released in daily change log
```

## 八、经验教训

1. **Windows 任务栏按钮图标 ≠ 窗口图标**：窗口带显式 AUMID 时，按钮图标走「AUMID → 开始菜单
   快捷方式匹配」，链上任何一环（残留快捷方式）都能劫持显示。
2. **dev 与正式版必须用不同 AUMID**：否则开发期的任何状态都会污染正式版的 Shell 解析。
3. **CSS 动画的静止态不能依赖动画本身**：`opacity: 0` + `animation: forwards` 的组合在
   `prefers-reduced-motion` 下等于永久隐形，隐藏态必须放 keyframes 的 `from`。
4. **"关窗口=进托盘"的应用必须重写安装器的运行检测**：默认 WM_CLOSE 流程杀不掉托盘常驻进程。
5. 排查 Shell/渲染问题时要逐环验证解析结果（`SHDefExtractIcon`、`ExtractIconEx`、lnk 的
   AUMID 属性、WM_GETICON 句柄渲染），而不是"改了源看显示有没有变"。
6. **发版前必须完整跑一次打包**：NSIS 宏的 LogicLib 配对错误只有真正编译才能暴露。

## 九、遗留事项

- [ ] Gitee 令牌 → 补发 v2.6.9 到 Gitee（更新器主源，国内用户更新提示依赖它）
- [ ] 新用户机器验证 2.6.9：隐身修复（动画效果=关的场景）+ 托盘运行时覆盖安装
- [ ] 开始菜单导入质量（混入卸载器/帮助类 lnk）与全局热键冲突警告——体验优化，未排期
