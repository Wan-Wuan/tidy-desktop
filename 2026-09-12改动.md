# 2026-09-12 项目改动记录

> 本日共 26 个提交，发布 4 个版本：**v2.5.0 → v2.6.0 → v2.6.5 → v2.6.6**
> 主线：应用图标全新设计 → 搜索框液态玻璃材质 → 新增玻璃主题 → 主界面全面玻璃化与无边框化 → 更新检测增强

---

## 一、应用图标全新设计

- 全新定稿图标：**奶油瓷收纳抽屉**（暖白瓷底 + 珊瑚/金双色磁贴，拟物收纳隐喻）
- 设计过程历经 12+ 个候选方向（磁贴网格/极简 T/文件夹/印章/鎏金/宝石/彩色磁贴墙等），最终由用户选定
- 统一应用到所有使用点：主窗口、Windows 任务栏、系统托盘、桌面快捷方式、浏览器 favicon、应用内品牌标识
- 新增图标生产管线：`scripts/generate-icons.mjs` + `scripts/icon-renderer.cjs`（Electron 离屏渲染 SVG 母版 → 全尺寸 PNG 16-512 + 多尺寸 ICO），一条命令重新生成

## 二、搜索框液态玻璃材质（深色烟熏）

- 全新深色烟熏玻璃：72% 暗底、`blur(32px) saturate(1.5)` 中度磨砂、26px 大圆角
- 液态玻璃细节：顶缘高光渐变带、内顶高光、悬浮层次
- 结果项玻璃化：悬停/激活微光玻璃块、暗色图标底座、薄荷绿辉光强调色
- 快捷键提示、滚动条、空态提示统一暗色玻璃语言
- 修复：四角隐约方角伪影（移除被窗口边界裁切的外投影 + `roundedCorners: false`）
- 修复：文字对比度按 macOS Spotlight 标准全面提亮

## 三、搜索框设置扩展

设置 → 搜索 新增/完善：

- **玻璃主题**：深色烟熏 / 浅色云瓷 双主题切换
- **玻璃透明度**：50%–95% 滑块，带 ↺ 恢复默认（72%）
- **显示底部提示**：开关，关闭后搜索框更紧凑
- 搜索框宽度、垂直位置、结果条数设置保留，均带恢复默认按钮

## 四、新增「玻璃」主题（第五主题）

- 高饱和**祖母绿→琥珀**暗底 + 翡翠/薄荷/金/珊瑚四色光斑
- 全部玻璃面板的磨砂、透色、边缘光在此背景下全显形
- 主题强调色换为祖母绿（与收纳抽屉图标一体）
- 霜玻璃浓度与文字色阶专门调校，保证彩色底上的可读性
- 极光球透明度收敛为点缀

## 五、主界面 macOS 玻璃化（浅 / 深双款）

- 浅色款：瓷白玻璃面板 `rgba(252,251,248,.5-.8)` + 白色高光描边
- 深色款：**纯暗底 + 烟熏玻璃**（去除全部背景干扰，与搜索框材质完全同源）
- 极光背景增强：新增琥珀/玫瑰光球（共 5 球）、顶部光井、细噪点纹理层（消除渐变色带）
- 玻璃面板配方升级：`blur(40px) saturate(1.8) brightness(1.08)`、透明度下调透出背景、上亮下暗渐变描边（mask 双层实现）、双内高光
- 卡片：悬停「拿起」效果（磨砂提亮 + 边缘光增强）+ 45° 光泽扫过微交互

## 六、完全无边框窗口

- 移除 Windows 原生标题栏与最小化/最大化/关闭按钮
- 头部玻璃**通栏贴顶**，成为窗口拖拽区（`-webkit-app-region: drag`，交互元素自动排除）
- 双击顶栏空白处最大化/还原；最大化自动填充工作区（不遮挡任务栏），还原回原尺寸
- 关闭入口：`Esc` / 托盘图标点击 / 托盘右键退出 / 任务栏右键关闭 / 工具条 ✕ 按钮
- 移除主界面底部提示栏，窗口为纯粹的玻璃内容区

## 七、工具条统一与无字模式

- 四个彩色按钮（添加应用/添加文件夹/整理中心/设置）统一为 **macOS 风格连体工具条**：单一玻璃底色 + 细分隔线 + 主题变量配色，悬停提亮
- 修复：深色主题悬停文字隐形、浅色主题悬停无反馈（悬停配色改用随主题翻转的变量）
- 新增**选项卡无字模式**（默认开启）：工具条只显示图标，更紧凑；末尾追加关闭窗口 ✕ 按钮（悬停红色提示，点击隐藏窗口）

## 八、更新检测增强

- 双平台（Gitee/GitHub）都探测后**自动取版本号更高的源**
- 解决场景：只发布了 GitHub 而未同步 Gitee 时，用户可一步更新到最新版，不再需要台阶式升级

## 九、其他修复

- 搜索框提示开关首次切换不生效（高度同步依赖缺 `config`）
- 主窗口版本徽章硬编码 `2.0` → 动态显示真实版本号
- README 技术栈版本修正（Electron 43 / Vite 8），新增完整发布流程文档
- 恢复被误删的 `search.html`（搜索窗口构建入口，必须保留）

## 十、发布记录

| 版本 | 主要内容 |
|---|---|
| v2.5.0 | 全新应用图标（奶油瓷收纳抽屉） |
| v2.6.0 | 玻璃主题、搜索框液态玻璃材质与设置、主界面玻璃化第一批 |
| v2.6.5 | 选项卡无字模式默认开启 |
| v2.6.6 | 更新检测双源取最新 |

## 十一、遗留注意事项

- 工作区存在文件被外部改写/回滚的情况（编辑器旧缓冲保存或同步盘冲突），**打包发布前务必 `git status` 核对**，发现异常回滚用 `git checkout -- <file>` 恢复
- 更新检测在两平台 API 限流同时触发时会报"检查失败"（匿名额度：GitHub 60 次/小时/IP），属平台限制，稍后重试即可
