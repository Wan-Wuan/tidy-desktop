# 2026-09-13 图标一致性修复

> 现象：应用内品牌图标已是新的「奶油瓷收纳抽屉」，但 Windows 任务栏仍显示 Electron 默认图标。

## 一、根因

### 1. 运行时用 PNG 当窗口图标（主因）

`src/main/index.ts` 里图标统一由 `getAppIcon()` 提供，而它只读 `build/icon-256.png`：

```ts
return nativeImage.createFromPath(path.join(__dirname, '../../../build/icon-256.png'))
```

Windows 的窗口/任务栏图标只认 `.ico`。把 PNG 交给 `BrowserWindow` 时窗口图标不会真正生效，
任务栏退回**进程默认图标**：

- 开发模式（进程是 `electron.exe`）→ 显示 Electron 内置图标 —— 正是本次截图的现象
- 安装版（进程是 `tidy-desktop.exe`）→ 恰好被 exe 内嵌图标兜住，所以问题只在开发/调试时暴露

同时，无边框窗口（`frame: false`）在 Windows 上不会套用构造参数里的 `icon`，需要创建后显式再设一次。

### 2. ICO 目录项字段不合规（隐患）

`scripts/generate-icons.mjs` 手工拼 ICO 时，目录项第 4、6 字节写成了 `wPlanes=32 / wBitCount=0`（应为 `1 / 32`），
`bColorCount` 写成 1（≥8bpp 应为 0）；且 7 个尺寸**全部**用 PNG 压缩条目 ——
Windows 只对 256×256 正式支持 PNG 条目，小尺寸应使用 BMP(DIB)，否则部分 Shell 图标 API 会判定条目不可用。

## 二、修复内容

| 文件 | 改动 |
|---|---|
| `src/main/index.ts` | 图标解析重写：Windows 优先取 `.ico`，按 `process.resourcesPath/build` → asar 内 `build/` 逐目录、逐候选名试探，逐个用 `isEmpty()` 校验后缓存；新增 `applyAppIcon()`，主窗口与搜索窗创建后显式 `setIcon()`；无可用图标时打警告而不是静默退化 |
| `scripts/lib/png.mjs` | 新增：8bit 非隔行 PNG 解码（RGBA 展开） |
| `scripts/lib/ico.mjs` | 新增：ICO 组装/解析；目录项按规范写 `wPlanes=1 / wBitCount=32 / bColorCount=0`；`dibFromPng()` 生成 BITMAPINFOHEADER + BGRA + AND 掩码的 BMP 条目 |
| `scripts/generate-icons.mjs` | 改用上述库：16/24/32/48/64/128 走 BMP 条目，256 走 PNG 条目；生成后自检结构，不合规直接抛错 |
| `scripts/check-icons.mjs` | 新增：校验脚本 —— 必备尺寸（16/32/48/256）、目录项字段、载荷类型与范围、`icon.ico` 与 `app-icon.ico` 同源、256 条目与 `icon-256.png` 同源、`public/` 分发一致。exe 内嵌校验默认关闭，需 `--exe` 显式开启（见下） |
| `electron-builder.yml` | `files` 增加 `build/app-icon.ico`，让 asar 内也有一份 ICO 兜底 |
| `package.json` | 新增 `icons:generate` / `verify:icons`；`electron:build` 串入 `verify:icons`，图标不合规直接拦住打包 |

## 三、验证

```bash
node scripts/generate-icons.mjs   # 重新生成 ICO（16~128 为 BMP 条目，256 为 PNG）
node scripts/check-icons.mjs      # 打包前：校验 ICO 结构 + 各使用点同源
# …打包…
node scripts/check-icons.mjs --exe   # 打包后：追加校验 exe 内嵌同一套图标
```

> **顺序很重要**：exe 校验必须在打包**之后**。打包前 `release/win-unpacked` 里躺的是上一版 exe，
> 拿新 ICO 去比对必然报"未内嵌图标"（首批实现就踩了这个坑，已改为默认跳过 + 旧产物自动跳过）。

- ICO 结构：7 个尺寸（16/24/32/48/64/128/256），字段合规，158,930 字节
- Electron `nativeImage.createFromPath('build/app-icon.ico')` 读取正常（256×256）
- `tsc -p tsconfig.main.json` 编译通过

## 四、补充：发行版仍复现的真正原因（同日追加）

上面两处修复后打出 2.6.7 实测：exe 内嵌、`resources/build/app-icon.ico`、窗口
`WM_SETICON`、窗口类图标、桌面/开始菜单快捷方式**全部已是新图标**，但任务栏按钮
依旧显示 Electron 默认图标。逐步实验定位：

1. 向运行中的窗口 `WM_SETICON` 发送纯红色测试图标 → 任务栏按钮**纹丝不动**；
2. 覆盖安装（快捷方式重建 + `SHCNE_ASSOCCHANGED` 通知）→ 无效；
3. 重启 Explorer → 无效；
4. 删除 `%LOCALAPPDATA%\Microsoft\Windows\Explorer\iconcache_*.db` 与
   `%LOCALAPPDATA%\IconCache.db` 后重启 Explorer → 图标恢复。

**结论（最终版）**：Explorer 解析任务栏按钮图标走的是
「窗口 AUMID → 开始菜单快捷方式匹配」。dev 调试时 Chromium 为通知功能自动创建了
`开始菜单\Programs\Electron.lnk`，旧版代码下它携带与发布应用相同的 AUMID
（`com.tidy-desktop.app`）且指向 node_modules 的 electron.exe——于是发行版的
任务栏按钮按 AUMID 匹配到了这个快捷方式，显示 electron.exe 的默认原子图标。
这解释了此前所有"无效"的修复尝试：窗口 `WM_SETICON`（红图标实验无反应）、
exe 内嵌图标、快捷方式 IconLocation、`SHChangeNotify`、重启 Explorer、
删除 iconcache 数据库，全都与这条匹配链无关。

**修复组合（本次新增/调整）**：

| 文件 | 改动 |
|---|---|
| `build/installer.nsh` | ① 升级安装时自动删除开始菜单残留的 `Electron.lnk`；② ICO 复制为带版本号的 `app-icon-${VERSION}.ico` 并让快捷方式指向它 |
| `src/main/index.ts` | `getAppIcon()` 候选名首位增加 `app-icon-${app.getVersion()}.ico`；**dev 使用独立 AUMID（`com.tidy-desktop.app.dev`）**，从根上避免 dev 实例再创建/污染与发布应用同 AUMID 的快捷方式 |

### 已中毒机器的一次性处置

- 删除开始菜单里的残留 `Electron.lnk`（`%APPDATA%\Microsoft\Windows\Start Menu\Programs\`），
  重启 Explorer 或重新启动应用即可恢复。
- 2.6.7 及以后的安装包在升级安装时会自动删除该残留。
- 开发模式（`electron:dev`）任务栏图标显示正常属预期（独立 AUMID，无快捷方式匹配，
  直接用窗口图标）。

## 五、遗留

- 若任务栏图标仍显示旧图标，先排除 **Windows 图标缓存**：重新安装后仍旧 → 取消任务栏固定再重新固定，或清理图标缓存。
- 发布安装包需要 `npm run icons:generate` → 版本号自增 → `npm run electron:build`。
