# 下一版优化建议

审查日期：2026-09-18
审查范围：`src/` 全部 63 个文件、约 12,600 行；外加构建/发布脚本与工程化配置。
主进程、渲染进程、工程化三条线分别过了一遍，下面的**每条都带文件:行号**，方便直接定位。

---

## 一句话结论

下一版最值得投的三件事：

1. **修更新安装的「黑洞」** —— 安装失败无日志、无提示，而诊断按钮指向一个永远不存在的文件；
2. **拆 App.tsx + 修 AppCard 的 memo 失效** —— 这是拖拽卡顿的真正原因，也是后续加功能的瓶颈；
3. **补数据损坏恢复 + 弹窗可访问性** —— 都属于「用户会真的遇到」但一直没补的洞。

---

## P0 · 真 bug，建议下一版必修

### 1. 更新安装失败 = 黑洞，且诊断入口是死的

证据链（已逐条确认）：

- `src/main/update/assistant.ts:98-102` —— spawn NSIS 后直接 `.unref()`，不看退出码、不写日志
- `src/main/update/installer.ts:79` —— spawn 成功 300ms 后就 `app.exit(0)`
- `build/installer.nsh` —— 全文**没有任何日志写入**（已 grep 确认）
- 但 `src/main/handlers/systemHandlers.ts:515` 的 `open-update-log` 去读 `%TEMP%\tidy-desktop-install.log`
- `docs/新用户全流程测试清单.md:101` 还要求排查时「必须附上」这个日志

**影响**：UAC 被取消、安装包被占用、NSIS 报错时，应用直接消失——无提示、无日志、无现场。用户只能反复重试，你也拿不到任何可排查的信息。这是最难处理的一类用户反馈。

**建议**：assistant 侧监听 `exit`/`error` 并写 `tidy-desktop-install.log`；失败时**不要先 exit**，保留主进程把错误回传渲染层；`open-update-log` 在文件不存在时给明确提示。

### 2. 右键拖拽读到过期的 config（闭包）

- `src/renderer/src/App.tsx:347` 的 useEffect，依赖数组在第 **454 行是 `[]`**
- 但内部调用了 `handleReorderApp`
- `handleReorderApp` 在 `1662` 行，其中 `1677` 行读 `config?.ui?.sortMode`
- 首次渲染时 `config` 还是 `null`，闭包**永久锁死在 `'manual'`**

**影响**：用户切到「按名称 / 启动次数」排序后，右键拖拽仍走手动 splice 分支，落点与实际显示顺序对不上。

**建议**：依赖数组补上 `handleReorderApp`，或用 ref 持有最新函数。

---

## P1 · 结构性，决定下一版之后还能不能愉快加功能

### 3. AppCard 的 `React.memo` 实际没生效（卡顿根因）

`src/renderer/src/components/AppCard.tsx:17` 确实包了 `React.memo`，但 `App.tsx` 传进去的回调**每次渲染都是新函数**：
`handleCardClick`(1142)、`handleCardMouseDown`(1208)、`handleCardContextMenu`(1220)、`handleCardDragStart`(1230)、`handleCardDragOver`(1254)、`handleCardDrop`(1266)、`handleCardDragEnd`(1283)、`handleCardKeyDown`(1311)；`onEdit`/`onDelete`/`onDragLeave` 还是内联箭头（2328-2335）。

拖拽时 `setDragOverAppId` 高频触发，memo 被击穿 → **每张卡片全量重渲染**。App.tsx 注释里反复担心的「网格会被反复重渲染到卡死」，根因就在这。

**改法（成本极低）**：上述回调全部 `useCallback` 包定（依赖注意用 ref），内联箭头提出去。顺带把 `isSelected={selectedAppIds.includes(app.id)}`(2326) 换成 `Set`，现在是 O(n²)。

### 4. App.tsx 2398 行，该拆了

单个组件同时承担：数据加载/持久化、**两套拖拽引擎**（HTML5 + 右键自绘）、右键菜单、多选、模态框编排、图标回填、撤销栈。26 个 `useState` + 20+ `useRef`，无 `useReducer`。

另外三组状态镜像在约 40 处手动双写：`apps`/`appsRef`(63,100)、`categories`/`categoriesRef`(64,101)、`activeCategory`/`activeCategoryRef`(66,102)。漏写任一处即不一致——这也是 `useMaintenance` 必须接一大串 ref 的根因。

**建议拆分顺序**：

| 抽出的东西 | 对应区域 | 类型 |
| --- | --- | --- |
| `useAppData()` | 62-66、192-227、540-581 | hook（收敛 ref 镜像） |
| `useDragAndDrop()` | 85-91、94-124、347-454、1230-1294、1543-1660 | hook |
| `useAppSelection()` | 326-327、1142-1206 | hook |
| `useCategoryDialogs()` | 322-324、1330-1402 | hook |
| `useIconBackfill()` | 494-538 | hook |
| `useUndoSnapshot()` | 92、150-158、1696-1716 | hook |
| `HeaderOverview` / `CategoryNav` / `AppGrid` / `SelectionBar` / `ToastStack` | 1832-2456 | 展示组件 |

拆完 App.tsx 可降到 **~500 行**。

### 5. IPC 入参校验与来源校验不完整（安全）

- `src/main/validation.ts` 只覆盖 config / apps / categories——这三处质量不错；
- 但 `appHandlers.ts:76/96/114/140/228/242`、`iconHandlers.ts:172/210/287` 的 `open-app`、`open-folder`、`open-app-as-admin`、`copy-file-to-clipboard`、`extract-icon` **直接使用未校验的路径字符串**；
- `src/main/preload.ts:5-83` 暴露约 **65 个 API**，其中含 `openAppAsAdmin`（提权）、`installUpdate(filePath)`、`runQuickAction`、`runUiCommand`、`startDragFile`；
- `ipcMain` 仅 3 处 resize handler 校验了 `event.sender`，其余均无。

现状不是「已被利用」，而是**渲染层一旦出问题就没有第二道防线**。建议抽 `assertSender(event)` + 路径类型/存在性统一校验入口。

### 6. 窗口安全还剩两个口子

- `sandbox` **没有显式声明**（依赖 Electron 默认值），建议写明 `sandbox: true`；
- `index.ts:112-122` 的 `isAllowedInternalUrl` 在生产环境**放行任意 `file:`** —— 本地任意 HTML 都能导航进来复用 preload 权限，建议收敛到自身 renderer 目录。

（`contextIsolation: true`、`nodeIntegration: false`、`webSecurity: true`、`setWindowOpenHandler` deny、`will-navigate` 拦截这几项都是对的。）

### 7. 数据安全：损坏后不可逆

- `src/main/jsonTransaction.ts:27-31` 的流程是「rename 原→.bak，rename tmp→原」，**没有 `fsync`**，回滚只覆盖同进程内的 `catch`(38-50)。若在两次 rename 之间断电/被杀，数据文件只存在于 `.bak`，**启动时没有任何恢复逻辑**；多文件事务还会出现新旧混合。
- `src/main/config.ts:49-56` 解析失败只落一个 `.corrupt-*` 就返回默认值，**没有任何 UI 恢复入口**，渲染层随后 `save-apps` 就会把默认值写回去。

对一个「用户攒了几百个应用和分类」的工具，这比崩溃更伤。

**建议**：启动时扫描残留 `.bak`/`.tmp` 做恢复；写后 `fsyncSync`；损坏时**禁止自动保存**并引导用户从 `.corrupt-*` 恢复。

### 8. 主进程同步阻塞（批量导入卡 UI）

- `iconHandlers.ts:94-141` `extractIconByPowerShell` 用 `execFileSync('powershell')`，超时 4000ms，经 `getIconPng`(165) 逐个调用
- `iconHandlers.ts:61` `getSteamInstallPaths` 用 `execFileSync('reg')`，2000ms
- `systemHandlers.ts:305-313` `validate-apps` 最多 5000 次同步 `existsSync`
- `config.ts:20-35` 启动时对 icons 目录逐个 `statSync`

**影响**：批量导入快捷方式时界面冻结数秒到数十秒。

**建议**：改异步 fs + 原子批量接口；PowerShell 兜底异步化并限流；启动扫描延后或走缓存清单。

---

## P2 · 顺手做

9. **删死代码**：`App.tsx:2119` 有个 `<section className="hidden">`，是一整块页头概览 + 智能启动的复制品，被 Tailwind 永久隐藏，里面还夹着一次 `smartLaunchApps` 的重复遍历（2119-2168）。直接删。

10. **可访问性缺口**：`AddAppModal`、`SettingsModal`、`SmartOrganizeModal` 都没有 `role="dialog"` / `aria-modal` / focus trap，且**按 Esc 关不掉**——因为 `App.tsx:338-340` 在 overlay 打开时直接 `return`。两个右键菜单里，`AppContextMenuOverlay.tsx:53` 有 `role="menu"` 但子项无 `role="menuitem"`、无方向键；`CategoryContextMenuOverlay`(CategoryOverlays.tsx:80) 连 role 都没有。只有 `CategoryDeleteDialogOverlay`(185-192) 做对了。建议抽 `useDialogA11y(ref)` 统一处理。

11. **没有 lint 工具链**：ESLint / Prettier / `.editorconfig` / husky **全部不存在**（已确认），而 `tsconfig.json` 还关掉了 `noUnusedLocals` / `noUnusedParameters`。App.tsx 这个体量最容易积死代码。建议至少上 ESLint（`warn` 起步渐进）+ Prettier，约半天。

12. **`typecheck` 用错了配置**：`npm run typecheck` 走 `tsconfig.json`（ESNext + bundler + DOM），而主进程真实编译走 `tsconfig.main.json`（CommonJS）——两边环境不一致可能漏报；`tsconfig.node.json` **没有任何地方引用**，形同闲置。建议拆成 `typecheck:main` / `typecheck:renderer`。

13. **重复代码**：`owner`/`repo` 硬编码散在 4 个脚本里（`publish-github-release.mjs:15`、`publish-gitee-release.mjs:4`、`sync-gitee-metadata.mjs:6`、`gitee-releases.mjs:21`，大小写还不一致）；搜索窗布局数学重复 4 处（`index.ts:38-43/366-369/486-496`、`systemHandlers.ts:191-199/236-244`）；`expandWindowsEnvPath` 两份；URL 安全判断 4 处。

14. **核心逻辑无测试**：`sortAppsForDisplay`(App.tsx:1410)、`handleReorderApp`(1662)、搜索匹配与打分（SearchApp.tsx:277/297）全都没测试——而这些恰恰是最容易改坏的地方。它们大多是纯函数，抽到 `utils/` 后补测**成本很低**（不需要 jsdom）。目前 `vitest.config.ts` 只收 `*.test.ts`，组件完全不可测。

15. **文档漂移**：`README.md:129-130` 安装示例还写着 v2.2.0；`design-qa.md` 结尾是 `final result: blocked`——三套界面模板的视觉保真度验证**一直没做完**（当时缺浏览器截图能力，现在有 Playwright 了）。`CHANGELOG.md` 停在 v2.7.1（你说 v2.7.2 是测试版，这条可先不管）。

16. **发布流程无 CI**：`.github/` 与 `.gitee/` 都不存在，打包 + 双渠道发布全靠本机手动。`release.mjs:146-150` 只打印命令不执行。风险点：双平台容易漏发一侧（而更新以 Gitee 为主源，漏发即国内断更）、`GITEE_TOKEN` 无法沉淀。
   现实做法：Gitee 没有官方 runner，所以「GitHub 侧全自动 + Gitee 侧保持脚本地推」是性价比最高的折中。最低成本先加一个跑 `typecheck + test` 的 workflow（约半天）。
   另外 `release.mjs:135` 打 tag 失败时不回滚，会留下「已提交无 tag」的半成品状态。

---

## 明确**不用动**的部分

避免过度重构，这些地方做得是对的：

- **拖拽的节流与守卫很扎实**：`useDragGhost.ts` 用 rAF + `will-change:transform` + 直接操作 DOM 绕开 React 重渲染；`dragOverGroupRef` / `groupInsertPlanRef` / `rightDragTargetRef` 避免 dragover 每帧 setState；`clearDragState` 在 await 前调用（含注释说明）规避了 dragend 丢失。
- **类型纪律好**：整个渲染进程只有 `useUpdate.ts:70,96,120` 三处 `catch (err: any)`。
- **更新器的核心逻辑过关**：哈希校验、降级清理、断点续传、仓库域名白名单、安装包路径锁定都处理对了。真正缺的只是**可观测性**（P0 第 1 条）。
- **脚本职责单一且幂等**：`check-icons.mjs`（exe 校验默认关、mtime 防误报）、`clean-main-build.mjs`、`wait-for-vite-electron.mjs`、`sync-gitee-metadata.mjs`。
- **依赖无废弃包**：Electron 43 + React 18 + Vite 8 + vitest 4 组合健康。唯一可对齐的是 `@types/node ^25` 可能新于 Electron 43 内置的 Node。
- **配置无冲突**：`electron-builder.yml` 的 maximum 压缩 + 关差分是刻意设计（有注释）。

---

## 下一版怎么排（按不同目标）

**如果这一版以「修问题、稳质量」为主**（推荐，用户可感知）
> P0 两条（第 1、2 条）+ 第 7 条数据恢复 + 第 9 条删死代码
> 两三天量级，直接减少用户反馈里的「更新装不上」和「数据丢了」。

**如果这一版以「重构、为后续铺路」为主**
> 第 3 条（半天，立竿见影）→ 第 4 条拆分（2-3 天）→ 第 14 条补测试
> 顺序不要反：先让 memo 生效，再拆文件，否则拆的过程中拖拽性能会更难判断。

**如果这一版要加新功能**
> 先做第 3 条。当前基础上每加一个卡片交互都会放大重渲染问题，先修再叠功能。

---

## 几个可以顺带考虑的方向

- **更新失败的用户反馈**：除了写日志，可以在设置里显示「上次更新失败原因」，把现在的黑洞变成可自助排查的路径。
- **数据恢复入口**：配合第 7 条，设置里加一个「从损坏备份恢复」，把已有但没人知道怎么用的 `.corrupt-*` 利用起来。
- **测试基础设施**：如果要开始给组件写测试，需要引入 jsdom + @testing-library/react，并调整 `vitest.config.ts` 的 include（目前只收 `*.test.ts`）。
- **视觉保真度收尾**：`design-qa.md` 的 blocked 状态建议正式关掉——用 Playwright 把三套模板在 1440×1024 下截一遍，确认或修正后归档那份文档。
