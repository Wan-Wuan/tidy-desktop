# 更新日志

记录 tidy-desktop 各版本的变更。版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

---

## v2.9.1 · 2026-09-19

移除上一版新加、但实际无用的「更新安装状态提示」功能（设置页横幅、安装日志状态查询、
相关 IPC 与入口），其余功能与 v2.9.0 完全一致。这是一次纯减负的小版本。

### 移除

- 「上次更新可能没有装完」横幅，及其背后的安装日志状态查询（`getUpdateInstallStatus`）、
  日志打开入口（`open-update-log`）与相关主进程 handler / preload 暴露 / 类型声明。
- 保留安装日志的**写入**逻辑（更新流程排错依据不变），仅不再读取并据此弹提示。

---

## v2.9.0 · 2026-09-19

拖拽体验的最后一轮打磨：修掉频繁拖拽时剩下的几处卡顿与交互缺陷；同时把「数据写入
失败被静默吞掉」这一问题族整体清掉——落盘失败从此可见。

### 修复

- **频繁拖拽排序时的卡顿 / 掉帧**
  卡片悬停过渡的「拖拽冻结」信号，此前要等指针越过 3px 位移阈值、再等一次渲染才
  生效——按下到拖动之间这段窗口里，快速划过一排卡片会同时新起十几条 220ms 过渡
  （每条都带模糊重算）。现在**按下瞬间即冻结**，拖拽期间悬停不再产生任何过渡动画。

- **拖拽后幽灵卡片残留**
  预览贴图入场动画有一个「过渡结束事件不触发时 260ms 兜底收尾」的定时器，此前未随
  贴图移除而取消，高频连续拖拽下旧定时器堆积。现在移除贴图时一并清理。

- **单击一次卡片后，所有卡片的悬停效果失效**
  上一条「按下即冻结」引入的回归：只点不拖时冻结信号没有复位。已在松手时复位。

- **拖拽松手落在空白处后，下一次点击卡片没反应**
  拖拽松手后浏览器会补发一次点击，程序用标志位拦住它；但松手落在空白处时那次点击
  不会经过任何卡片，标志位没人消费、一直悬着，把之后第一次正常点击吃掉（须点两次）。
  现在补上了 500ms 自愈兜底。

- 其他复查修正：外部文件拖到分类上导入时保存失败不再静默；清掉两处只写不读的
  残留引用与一处误导性注释。

### 新增

- **保存失败可见**
  应用 / 分类 / 配置数据写入磁盘失败时（磁盘空间不足、数据文件损坏、进程通信故障等）
  会弹出轻提示，不再静默丢失改动。覆盖拖拽排序、归类、批量操作、导入、快捷方式导入、
  撤销等全部写路径；搜索窗口（Ctrl+K）内的改动同样接入。撤销失败时明确提示
  「撤销未完全落盘，重启后可能回退」，而不是照常宣布「已撤销」。

### 重构

- **拖拽引擎整体迁出主文件**：手势、落点判定、收尾逻辑搬入 `hooks/useDragAndDrop.ts`，
  幽灵贴图与数据动作以参数注入。`App.tsx` 2156 → 1775 行。落点判定与重排的纯函数
  （`utils/dropTarget.ts`、`utils/reorder.ts`）共有 26 个测试锁定。
- 数据落盘统一收敛到 `utils/persist.ts`：主进程用「返回 false」表达失败（如数据文件
  损坏时拒绝写入），此前渲染层全部忽略该返回值；现在除刻意保留的双文件原子事务与
  高频非关键路径外，全部走统一封装。

---

## v2.8.5 · 2026-09-19

面向内部质量的整理：拆分巨型组件、清掉全部 lint 告警，并修掉清理与搬迁过程中发现的
几个真问题。

### 重构

- **`App.tsx` 开始拆分**：2655 → 2156 行。抽出 5 个自定义 hook（`useAppData`、
  `useAppSelection`、`useCategoryDialogs`、`useIconBackfill`、`useUndoSnapshot`）
  与 5 个展示组件（`AppGrid`、`CategoryNav`、`HeaderOverview`、`SelectionBar`、
  `ToastStack`）。
  拖拽引擎**仍留在主文件**，留待后续单独一步搬迁——那是最近几轮修复最密集的地方，
  单独处理才能保证一旦出问题可以归因。
- 搬迁过程中逐条核对了拖拽的全部不变量并保留：清理时序、落点 plan 先读后清、
  ref 守卫、单击抑制、位移阈值、30 秒看门狗、落点判定收敛到每帧一次，
  以及稳定回调对 `AppCard` memo 的保护（`isSelected` 仍是 `Set.has`）。

### 修复

- **拖应用排序 / 归类的落点位置不准**
  落点「插到目标前还是后」的判断，原先由 HTML5 分组处理器算好写进一个 ref 并传给
  重排；拖拽统一到左键自绘引擎时那套处理器被整体删除，ref 从此只剩写入与清空、
  **无人读取**，而重排调用也漏传了该参数——于是不管把卡片拖到目标卡片的哪一半，
  它都会被插到**前面**。
  现在判定规则抽成纯函数 `isPointerPastMidpoint` 并有测试锁定；拖拽过程中实时计算，
  并在目标卡片内侧画一条**插入位置指示线**，做到「线画在哪、松手就落在哪」；
  松手时按松手位置再算一次，避免最后一帧之后的位移造成偏差。
  非手动排序时（顺序由排序规则决定）不画该指示线，以免承诺一个不会发生的落点。

- **添加应用弹窗会重置你选好的分类**
  该弹窗用于校正分类的 effect 依赖了分类列表，而分类列表的引用在保存数据后会变化，
  于是每次都把已选分类改回默认值——先选分类再填写名称，分类可能悄悄变回去。
  现在只在「已选分类确实失效（例如被删除）」时才回落。

### 改进

- **ESLint 告警从 22 条清零**。其中两处值得记下：`AppCard` 里导出的普通函数
  （`isDocFile` / `canNativeDrag`）挪到了 `utils/fileKind.ts`，因为组件文件里导出
  非组件会让热更新失效；另有一处「加了 `useCallback` 但只挂在 DOM 上、依赖又都是
  每次渲染重建的普通函数」的写法去掉了无谓的记忆化，反而让回调每次都用最新闭包。
- 补上 `npm run lint` 脚本（此前只有 Prettier，lint 一直是手动调用的）。
- 开发构建新增 DevTools 入口：主窗口是无边框且关闭了应用菜单，Electron 默认的
  `Ctrl+Shift+I` / `F12` 都不生效，导致开发时读不到渲染层的控制台日志。现在自己绑定
  这两个快捷键，仅开发构建生效，打包后不带这条路径。
- **右键菜单补上键盘可达性**：应用菜单的子项此前只是裸按钮（屏读不会当作菜单项），
  分类菜单连 `role="menu"` 与焦点环都没有。现在两个菜单都带 `role="menu"` /
  `aria-label`，子项为 `role="menuitem"`，并支持上下方向键在菜单项之间循环——
  「移动到分类」展开后的长列表从此不必靠 Tab 逐个走。

---

## v2.8.0 · 2026-09-18

这一版以「把看不见的问题修掉」为主：更新失败的现场、数据损坏的兜底、拖拽卡顿的根因、
以及一批长期缺失的工程化基础设施。

### 拖拽

- **三套拖拽引擎统一为一套左键方案**
  此前排序 / 归类靠左键 HTML5 拖拽（受浏览器节流、反馈迟钝），右键自绘拖拽负责灵敏
  操作，文件发送又走右键原生拖拽——同一个动作在不同按钮上是完全不同的行为。
  现在全部统一到左键：拖动卡片即可排序、归类；图片 / 文档拖出窗口即交给微信、
  浏览器等外部应用发送上传；拖拽全程留在窗口内则完成归类。右键只保留菜单。
  统一过程中还修掉了三个只有左键才会暴露的问题：拖完松手浏览器会补发 click 误开
  应用（加抑制标志）、单击时手一抖就弹出系统拖拽（加位移阈值）、以及旧实现里
  读取过期排序模式的闭包问题。

- **频繁拖拽卡顿的真正根因**
  拖拽预览卡片带 `backdrop-filter`，而它背后的画面（分组高亮底色的过渡、卡片 hover
  的 220ms 过渡）每帧都在变，导致浏览器每帧全量重算上百张卡片的模糊。
  现在拖拽期间冻结背后的一切变化，模糊命中缓存；落点判定也收敛到每帧一次，
  不再每次 mousemove 都强制布局。快速拖动时预览严格贴手。

- **拖拽子分类时落点差一位**
  子分类重排先删除再插入，但插入下标沿用了删除前的值——从前往后拖会整体偏一格。
  现在与应用重排同一语义（移除后重新定位插入点），并按松手位置在目标上的左右半边
  决定插到前还是后，拖到哪就停在哪。

- **Esc 关闭弹窗后焦点环残留**
  归还焦点时压环的标记会被任意后续按键提前撤销，而元素仍持有焦点，环恰好在那一刻
  出现并挂着。现在标记只在元素真正失去焦点时撤销；弹窗打开时的自动聚焦
  （设置 / 整理中心的关闭按钮）在鼠标操作下也不再出现焦点环，键盘用户的焦点环保留。

### 修复

- **更新安装失败不再是黑洞**
  此前安装助手派生安装器后直接撒手，既不检查退出码也不写日志，而「打开更新日志」按钮
  指向的 `%TEMP%\tidy-desktop-install.log` **全仓库没有任何代码写入**——安装失败时应用
  直接消失，用户和你都拿不到现场。现在从主进程交棒、等待主进程退出、启动安装器到
  安装器退出，每一步都落盘并带时间戳与退出码；日志自动轮转，不会无限增长。
  「打开更新日志」也会区分「本次没有走到安装步骤」与「打开失败」，不再点了没反应。
  应用启动时会读取上次结果，如果安装没走完，设置页会明确提示。

- **拖拽卡顿的根因：`AppCard` 的 `React.memo` 形同虚设**
  组件虽然包了 `memo`，但上层每次渲染都传入新的回调引用，memo 被完全击穿；拖拽时
  `dragover` 每秒触发几十次 state 更新，导致**每一张卡片全量重渲染**。现在 12 个回调
  全部改为标识稳定的版本，选中判定也从 `includes`（整体 O(n²)）换成 `Set`。

- **数据文件损坏后被静默覆盖**
  解析失败时只落一个 `.corrupt-*` 留档就返回默认值，随后渲染层一保存就把默认值写回去，
  而用户完全不知道备份存在。现在损坏文件在本次会话内**拒绝写入**，设置页会列出所有
  留档并提供「恢复」入口。

- **写入被中断后数据「看起来丢了」**
  原子写的回滚只在同一进程内生效；若在两次 rename 之间断电或被强杀，主文件会缺失而
  内容还留在 `.bak` / `.tmp` 里。现在写入补了 `fsync`，启动时会扫描残留并自动恢复。

- **弹窗按 Esc 关不掉**
  `App.tsx` 的全局 Escape 处理器在弹窗打开时会直接 return（这一步本身是对的，否则会连
  主窗口一起隐藏），但各弹窗自己从未处理 Esc。现在新增统一的对话框可访问性 hook：
  `role="dialog"` / `aria-modal`、焦点进入与归还、Tab 循环限制、Escape 关闭，已接入
  添加应用、设置、整理中心三个弹窗。

- 删除 `App.tsx` 中一整块被 `hidden` 永久隐藏的页头概览副本（含一次重复遍历）。

### 安全

- **IPC 来源与入参校验**
  全部 49 处 IPC 注册此前只有 3 处（窗口尺寸）校验过调用来源，而 preload 暴露了 60 多个
  方法，包含提权启动程序、安装任意 exe、关机重启、读写剪贴板、按任意路径提取图标。
  现在新增统一的来源校验与路径校验，并优先覆盖 8 个高风险入口；`open-app-as-admin`
  额外限制为可执行文件白名单。
- **渲染进程沙箱显式开启**，不再依赖 Electron 的默认值。
- **内部页面放行范围收敛**：生产环境此前放行任意 `file:`，意味着本地任意 HTML 都能被
  导航进来并复用 preload 的全部能力；现在限定在自身渲染层目录内。
- 顺带把散落四处的 URL 判断收敛到单一模块。

### 改进

- **主进程不再同步阻塞**：图标提取的 PowerShell 兜底、Steam 路径读取改为异步；
  `validate-apps` 由最多 5000 次同步存在性检查改为分批并发；启动时的图标缓存清理
  挪到异步执行。
- **工程化**：引入 ESLint（含 `react-hooks` 插件，可在编辑期抓出上面那类闭包问题）与
  Prettier；`typecheck` 拆为主进程 / 渲染进程两条，避免配置不一致导致漏报；
  新增 GitHub Actions 做 `typecheck + test + build` 质量门禁。
- **发布脚本**：仓库地址收敛到单一模块；`release.mjs` 在打 tag 失败时会回滚提交，
  并校验 CHANGELOG 是否已含当前版本；打包前清理 `release/` 里的旧产物。
- **测试**：从 11 个文件 / 约 50 个用例增加到 **16 个文件 / 81 个用例**，新增排序、
  搜索打分、拖拽重排、中断写入恢复的纯函数测试，以及一个锁定 `AppCard` memo 行为的
  组件测试（含反向用例，避免假绿）。

---

## v2.7.1 · 2026-09-17

### 改进

- **安装与导入不再依赖 PowerShell**
  安装包结束常驻进程改为使用安装器内置的能力，不再调用外部命令；
  导入快捷方式也改用系统接口直接读取，不再为每个快捷方式启动一次 PowerShell。
  结果是导入更快，且此前会闪出的命令行窗口彻底消失——在 PowerShell 被策略禁用
  甚至不存在的系统上，快捷方式图标也不会再丢失。

---

## v2.7.0 · 2026-09-16

### 改进

- **自动更新不再依赖系统脚本宿主**
  更新安装的最后一步，此前需要借助外部脚本宿主来等待进程退出并启动安装器。
  在这类组件被禁用、锁死或缺失的机器上（组策略限制、精简版系统等），
  更新会卡在"下载完成了却装不上"。现在改由应用自身派生一个安装助手进程来承担，
  等待与启动全部使用内置能力，任何环境下都能完成自动安装。

### 修复（深色主题）

- **文件夹图标在深色主题下看不清**
  文件夹、Steam 应用的图标底色此前是写死的浅橙 / 浅紫，深色主题下既刺眼，
  又让上面的图标几乎糊进背景。现在图标底色与图形颜色都跟随主题变化。

- **部分面板文字对比度不足**
  设置、分类管理等弹窗中的浅色区块，在深色主题下会混成灰紫色，
  叠加已转为浅色的文字后对比度仅约 3:1（低于无障碍标准要求的 4.5:1）。已修正。

- **卡片名称在深色主题下难以辨认**
  卡片名此前使用固定的深灰色，压在深色卡片上几乎看不见，现已跟随主题。

- **空状态图标不再写死颜色**，随主题自动适配。

---

## v2.6.10 · 2026-09-14

### 新增

- **拖拽归类**
  把应用直接拖到子分类分组上即可归类，**拖到哪个位置就排在哪个位置**；
  也可以拖进尚未添加应用的子分类。拖到左侧分类上可完成跨分类移动。

- **整卡拖拽预览**
  拖动时跟随鼠标显示完整的卡片预览，替换掉浏览器默认的粗糙虚影。

- **支持更多文件类型**
  在原有格式之外新增 `.md` / `.html` / `.json` / `.yaml` / `.log`，
  以及常见的代码文件、字体、音频、视频格式，均可添加为项目。

- **复制提速**
  复制文件、图片到剪贴板改用系统原生方式，不再调用外部命令，瞬时完成。

### 改进

- 浅色主题下的悬停效果此前几乎不可见，现已明显。
- 键盘焦点环恢复可见（此前被自定义样式覆盖，键盘操作时看不到焦点位置）。

### 修复

- 玻璃主题下部分文字与卡片操作按钮不可读（白底白字 / 深底深字）。
- 浅色主题、深色主题下的多处文字对比度不足。
